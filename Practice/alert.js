function loadHello(){
  alert("Hello!");
}

function sum(){
    let x = 3;      // ← xという箱を作り、その中に3を入れろ！という命令
    let y = 4;      // ← yという箱を作り、その中に4を入れろ！という命令
    let z = x + y;  // ← zという箱を作り、その中にxの中身（3）とyの中身（4）を足した値を入れろ！という命令
    alert(z);       // ← zの中身をアラートで表示しろ！という命令
  }